import { Form } from "../card/card";

class DetailsForm extends Form {
  constructor() {
    super();
    //get room number maybe?
    //get price per day
    //room-info__price = price;
    //in summary-list__price:
    //summary-list__key = `${price} x ${duration}`;
    //summary-list__value = `${price*duration}`;
    //get summary list
    //and total value
  }
}
